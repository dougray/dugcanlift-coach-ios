import SwiftUI

/// STUB — replaced with the real week-planning screen in Task 8.
struct CookPlanView: View {
    @Binding var clientID: String
    @Binding var weekStart: String
    @Binding var shareLink: String

    var body: some View {
        Text("Plan")
    }
}
