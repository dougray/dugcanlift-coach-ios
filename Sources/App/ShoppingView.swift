import SwiftUI

/// STUB — replaced with the real shopping-list screen in Task 9.
struct ShoppingView: View {
    @Binding var clientID: String
    @Binding var weekStart: String

    var body: some View {
        Text("Shopping")
    }
}
